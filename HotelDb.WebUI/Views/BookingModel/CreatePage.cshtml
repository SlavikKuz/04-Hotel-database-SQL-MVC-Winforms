@model HotelDb.WebUI.Models.BookingModel

@{
    ViewData["Title"] = "Create";
}

<form asp-action="Create">
    <div class="col-md-6 mx-auto">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-row">
            <label asp-for="Client" class="control-label"></label>
            <select asp-for="Client" class="form-control" asp-items="ViewBag.ClientList"></select>
        </div>


        @*<div class="form-row">
            <label asp-for="Booking.Id" class="form-control-plaintext"></label>
            <div class="form-group col-md-9">
                <select asp-for="Booking.Id" asp-items="Model.SelectListClient" class="form-control"></select>
                <span asp-validation-for="SelectListClient" class="text-danger"></span>
            </div>
            <input type="button" class="btn btn-primary mb-2 mx-auto" value="New Client" onclick="location.href='@Url.Action("Create", "ClientModel")'" />
        </div>

        <div class="form-row">
            <div class="col">
                <div class="form-group">
                    <label asp-for="Booking.DayFrom" class="form-control-plaintext"></label>
                    <input asp-for="Booking.DayFrom" class="form-control" />
                    <span asp-validation-for="Booking.DayFrom" class="text-danger"></span>
                </div>
            </div>
            <div class="col">
                <div class="form-group">
                    <label asp-for="Booking.DayTill" class="form-control-plaintext"></label>
                    <input asp-for="Booking.DayTill" class="form-control" />
                    <span asp-validation-for="Booking.DayTill" class="text-danger"></span>
                </div>
            </div>
        </div>


        <div class="form-row">
            <label class="form-control-plaintext">Guest Name</label>
            <div class="input-group mb-2 col-md-9">
                <select asp-for="GuestId" asp-items="Model.SelectListGuest" class="form-control"></select>
                <span asp-validation-for="GuestId" class="text-danger"></span>
                <div class="input-group-append">
                    <input type="submit" name="bookingButton" value="AddGuest" class="btn btn-light btn-sm" />
                </div>
            </div>
            <input type="button" class="btn btn-primary mb-2 mx-auto" value="New Guest" onclick="location.href='@Url.Action("Create", "ClientModel")'" />
        </div>


        <div class="form-row">
            @Html.DisplayNameFor(model => model.GuestList)
            <div class="form-control" readonly>
                @foreach (var item in Model.ShowSelectedGuests)
                    @(item + ", ")
            </div>
        </div>

        <div class="form-row">
            @if (Model.GuestList != null)
                for (var i = 0; i < Model.GuestList.Count; i++)
                    @Html.HiddenFor(model => Model.GuestList[i].ClientId)
        </div>

        <br>

        <div class="form-row">
            <div class="input-group mb-2 col-md-9">
                <select asp-for="RoomId" asp-items="Model.SelectListRoom" class="form-control"></select>
                <span asp-validation-for="RoomId" class="text-danger"></span>
                <div class="input-group-append">
                    <input type="submit" name="bookingButton" value="AddRoom" class="btn btn-light btn-sm" />
                </div>
            </div>
            <div class="form-check mb-2 form-check-inline mx-auto">
                <input class="form-check-input" type="checkbox" asp-for="Booking.WithKids">
                <label asp-for="Booking.WithKids" class="form-check-label"></label>
            </div>
        </div>

        <div class="form-row">
            @Html.DisplayNameFor(model => model.RoomList)
            <div class="form-control" readonly>
                @foreach (var item in Model.ShowSelectedRooms)
                    @(item.ToString() + ", ")
            </div>
        </div>
        @if (Model.RoomList != null)
            for (var i = 0; i < Model.RoomList.Count; i++)
                @Html.HiddenFor(model => Model.RoomList[i].RoomId)


        <div class="form-row">
            <label asp-for="Booking.Info" class="form-control-plaintext"></label>
            <input asp-for="Booking.Info" class="form-control" />
            <span asp-validation-for="Booking.Info" class="text-danger"></span>
        </div>*@

        @*<div class="form-row">
            <label asp-for="Invoice.TotalPrice" class="form-control-plaintext"></label>
            <div class="form-control mb-2 col-md-9">
                @Model.Invoice.TotalPrice
            </div>
            <input type="hidden" id="Invoice.TotalPrice" name="Invoice.TotalPrice" value="@Model.Invoice.TotalPrice" />
            <input type="submit" name="bookingButton" value="Invoice" class="btn btn-primary mb-2 mx-auto" />
        </div>*@
        <br>
        <div class="form-row ">
            <input type="submit" name="bookingButton" value="Save" class="btn btn-danger mx-auto" />
        </div>

    </div>
</form>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
